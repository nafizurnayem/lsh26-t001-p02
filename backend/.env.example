# Local development
PORT=4000
DATASET_PATH=../P02_pharmacy_expiry_public.json
DATA_FILE_PATH=./data/medicines.json
SEED_CASE_ID=PUB-12

# CORS: comma-separated list of allowed origins, or "*" for any origin.
# In production set this to your deployed frontend URL, e.g.
# CORS_ORIGIN=https://your-frontend.vercel.app
CORS_ORIGIN=*

# Set automatically by Vercel; forces the writable data file into /tmp.
# VERCEL=1
